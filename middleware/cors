const cors = require('cors');

const corsOptions = {
  origin: [
    'https://kazi-ocha-frontend.vercel.app',
    'http://localhost:3000',
    'http://127.0.0.1:5500',
    'http://localhost:5500'
  ],
  credentials: true,
  optionsSuccessStatus: 200
};

module.exports = cors(corsOptions);
